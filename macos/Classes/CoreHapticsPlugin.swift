// Placeholder file to mark macOS support for the dart-only plugin.
// Core Haptics is provided via the Swift Package located at ios/Package.swift,
// which includes macOS (.v11) in its platforms. Add that package to your macOS
// host app target in Xcode so the exported @_cdecl symbols are linked.

